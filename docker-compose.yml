services:
  db:
    image: postgres:17-alpine
    container_name: ASuServicio-db
    environment:
      POSTGRES_USER: sepc1012
      POSTGRES_PASSWORD: docker123
      POSTGRES_DB: ASuSevicio_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
